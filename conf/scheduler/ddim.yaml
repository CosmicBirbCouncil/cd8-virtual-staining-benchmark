name: ddim

scheduler:
  _target_: diffusers.DDIMScheduler
  num_train_timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02
  beta_schedule: linear
  clip_sample: true
  prediction_type: ${model.training.prediction_type}
  set_alpha_to_one: true
  steps_offset: 0
num_inference_timesteps: 50